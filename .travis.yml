language: java

compiler:
  - gcc

jdk:
  - oraclejdk8

env:
  - OPENCV_HOME=$HOME/opencv OPENCV_JAVA_PATH=$OPENCV_HOME/share/OpenCV/java

cache:
  directories:
    - $OPENCV_JAVA_PATH

install:
  - ./install-opencv.sh

script:
  - ./gradlew clean test
